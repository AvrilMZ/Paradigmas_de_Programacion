Escribir las siguientes expresiones con el menor número de paréntesis posible:
1. $(\lambda x.(\lambda y.(\lambda z.((x \space z) \space (y \space z)))))$  
2. $(((a \space b) \space (c \space d)) \space ((e \space f) \space (g \space h)))$  
3. $(\lambda x.((\lambda y.(y \space x)) \space (\lambda v.v) \space z) \space u) \space (\lambda w.w)$  


Respuesta:

1. $\lambda xyz.(x \space z) \space (y \space z)$
2. $((a \space b) \space (c \space d)) \space ((e \space f) \space (g \space h))$
3. $(\lambda x. (\lambda y.yx) \space (\lambda v.v) \space zu) \space (\lambda w.w)$ 